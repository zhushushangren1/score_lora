# LoRa 裁判记分系统使用说明书

本说明面向现场工作人员和客户。正常使用不需要电脑、不需要打开设备外壳，也不需要了解程序。

## 1. 设备组成

一套系统包含：

- 服务端 1 台：负责接收裁判端比分、提供 WiFi 热点、显示控制网页。
- 裁判端 3 台：每位裁判使用 1 台，用按键录入红蓝双方比分。
- 手机、平板或电脑 1 台：连接服务端 WiFi 后打开网页进行控制或显示。

服务端有 4 个状态灯：

| 状态灯 | 含义 |
|---|---|
| 电源灯 | 设备已上电 |
| 工作灯 | 固件正在运行，正常情况下会闪烁 |
| TX 灯 | 服务端正在发送 LoRa 数据 |
| RX 灯 | 服务端收到裁判端 LoRa 数据 |

裁判端有一个四位数码管、5 个计分按钮和一个低电量指示灯。

## 2. 开机顺序

推荐按以下顺序开机：

1. 打开服务端电源，等待电源灯常亮、工作灯开始闪烁。
2. 打开 3 台裁判端电源。
3. 用手机、平板或电脑连接服务端 WiFi。

服务端 WiFi 信息：

```text
WiFi 名称：ScoreServer
WiFi 密码：score1234
网页地址：http://192.168.4.1
```

打开 `http://192.168.4.1` 默认进入显示页。控制管理请打开：

```text
http://192.168.4.1/control
```

## 3. 页面说明

### 显示页

显示页用于大屏或观众端，只显示：

- 红队名称；
- 蓝队名称；
- 总比分；
- 倒计时。

显示页不提供操作按钮，避免现场误操作。

### 控制页

控制页用于工作人员管理比赛，主要功能包括：

- 查看 3 台裁判端是否在线；
- 查看每台裁判端本轮是否已提交；
- 绑定或解绑裁判端；
- 修改红队、蓝队名称；
- 进入下一轮并设置倒计时；
- 重置比赛比分。
- 查看最近事件，例如绑定、解绑、提交、下一轮、重置和通信异常。

控制页中的在线状态会自动刷新，不需要频繁手动刷新网页。

## 4. 首次绑定裁判端

首次使用或更换裁判端后，需要把裁判端绑定到 `client1`、`client2`、`client3`。

操作步骤：

1. 服务端和裁判端全部开机。
2. 手机或电脑打开控制页：`http://192.168.4.1/control`。
3. 在“未绑定设备”区域找到新出现的设备。
4. 选择绑定到 `client1`、`client2` 或 `client3`。
5. 绑定成功后，裁判端数码管会短暂显示 `J1`、`J2` 或 `J3`。

说明：

- 每台裁判端只需要绑定一次。
- 断电重启后绑定关系会自动保存。
- 如果绑定错了，可以在控制页解绑后重新绑定。

## 5. 裁判端计分操作

裁判端 5 个按钮含义如下：

| 按钮 | 短按 | 长按 |
|---|---|---|
| 红 +1 | 红队加 1 分 | 红队减 1 分 |
| 红 +2 | 红队加 2 分 | 红队减 2 分 |
| 蓝 +1 | 蓝队加 1 分 | 蓝队减 1 分 |
| 蓝 +2 | 蓝队加 2 分 | 蓝队减 2 分 |
| 提交 | 提交当前比分 | 清空本机当前输入 |

注意：

- 分数最低为 0，不会减成负数。
- 提交后，本轮裁判端会锁定，不能继续修改本轮比分。
- 进入下一轮后，裁判端自动解锁。
- 如果数码管显示 `SEND`，表示正在发送，请等待结果。
- 如果数码管显示 `Err`，表示本次提交没有得到确认，可联系工作人员检查服务端状态后重试。

## 6. 比赛流程

推荐比赛流程：

1. 工作人员打开控制页，确认 3 台裁判端均在线。
2. 根据需要修改红队和蓝队名称。
3. 每位裁判在裁判端录入本轮红蓝比分。
4. 每位裁判短按“提交”按钮。
5. 控制页确认各裁判端均已提交。
6. 系统自动统计总比分。
7. 工作人员点击“下一轮”，输入倒计时秒数并确认。
8. 显示页开始新倒计时，裁判端解锁，进入下一轮。

当前版本中，点击“下一轮”会进入新轮次，并重置显示用的总比分。

## 7. 服务端实体按钮

服务端有两个实体按钮：

| 按钮 | 操作 |
|---|---|
| 下一轮 | 短按进入下一轮 |
| 重置 | 长按 3 秒重置比赛状态 |

网页控制页和实体按钮功能可以同时使用。正式比赛建议优先使用网页控制页，因为网页会显示确认提示和当前状态。

## 8. 关机和充电

关机步骤：

1. 比赛结束后先关闭 3 台裁判端。
2. 再关闭或拔掉服务端电源。

裁判端使用 18650 电池供电。低电量灯闪烁时，应尽快更换或充电。

注意：

- 不要在设备开机时随意拆装内部线路。
- 不要让电池正负极短路。
- 不要使用损坏、鼓包或漏液的电池。
- 服务端建议使用稳定的 5V/2A USB 电源适配器。

## 9. 常见问题处理

### 手机打不开网页

检查：

1. 手机是否连接到 `ScoreServer` WiFi。
2. 浏览器地址是否为 `http://192.168.4.1`。
3. 服务端电源灯是否常亮、工作灯是否闪烁。

### 控制页显示裁判端离线

处理：

1. 确认裁判端已开机。
2. 确认裁判端天线已拧紧。
3. 将裁判端靠近服务端测试。
4. 重新启动该裁判端。
5. 如果仍然离线，联系维护人员检查 LoRa 模块和接线。

### 裁判端提交后显示 Err

处理：

1. 等待几秒，查看控制页是否已经收到该裁判端比分。
2. 如果控制页已经收到，等待进入下一轮即可。
3. 如果控制页没有收到，靠近服务端后重新提交。
4. 多次失败时，联系维护人员检查无线通信。

### 点击下一轮后有裁判端没有解锁

处理：

1. 再次点击下一轮前，先确认是否真的需要进入新轮次。
2. 如果只是个别裁判端未解锁，可等待它下一次收到服务端状态。
3. 必要时重启该裁判端，它会通过服务端状态恢复当前轮次。

### 比分或队名显示不更新

处理：

1. 确认手机仍连接 `ScoreServer` WiFi。
2. 手动刷新网页。
3. 如果仍不更新，重启浏览器或重新连接 WiFi。

## 10. 维护人员串口命令

以下命令仅供维护人员通过 USB 串口调试使用，普通现场操作不需要使用。

服务端命令：

```text
list
bind <deviceId> client1|client2|client3
unbind client1|client2|client3
next-round [seconds]
reset
lora-debug on
lora-debug off
```

裁判端命令：

```text
show
submit <red 0-99> <blue 0-99>
lora-debug on
lora-debug off
```

正常比赛时建议关闭 LoRa 调试输出：

```text
lora-debug off
```
